# هم‌ساختمان 🏢

اپلیکیشن مدرن و فارسی مدیریت ساختمان — برای ساکنین، مالکین و مدیران ساختمان.

---

## ۱. چشم‌انداز محصول

زندگی آپارتمانی با چالش‌های تکراری همراه است: پرداخت دیرهنگام شارژ، بی‌خبری از اطلاعیه‌ها،
هماهنگ‌نشدن تعمیرات و نبود شفافیت مالی. **هم‌ساختمان** این تجربه را به یک اپ ساده، شفاف و دوستانه تبدیل می‌کند:

- **شارژ و شفافیت مالی**: تعریف دوره‌های شارژ، ثبت دستی پرداخت‌ها، ثبت هزینه‌ها با رسید و نمای شفاف درآمد/هزینه/مانده
- **اطلاعیه‌های ساختمان** با اهمیت (عادی / مهم) و تصویر اختیاری
- **گزارش مشکلات ساختمان** (آب، برق، آسانسور، گاز، مشاعات، نظافت، سایر) با وضعیت رسیدگی
- **دایره خدمات ساختمان**: پیدا کن ← اطلاعات را ببین ← تماس بگیر؛ با نشان «مورد اعتماد ساختمان»
- **مدیریت ساختمان**: ساخت ساختمان، افزودن واحدها، دعوت ساکنین با کد دعوت، تخصیص اعضا به واحدها

اصول محصول:

| اصل | معنا |
| --- | --- |
| سادگی | هر کار ضروری حداکثر ۲–۳ لمس |
| اعتماد | شفافیت مالی و وضعیت‌های روشن |
| موبایل‌فرست | طراحی برای ۳۶۰ تا ۴۳۰ پیکسل، سپس تبلت و دسکتاپ |
| فارسیِ اصیل | راست‌به‌چپ واقعی، ارقام و تقویم جلالی، فونت وزیرمتن |

---

## ۲. پشته فنی

| لایه | فناوری |
| --- | --- |
| فریم‌ورک | **Nuxt 4** (Vue 3، TypeScript، SSR) |
| استایل | **Tailwind CSS v4** (CSS-first، بدون فایل کانفیگ) |
| کتابخانه کامپوننت | **Nuxt UI v4** (Reka UI) با پالت شخصی‌سازی‌شده |
| آیکون‌ها | **@nuxt/icon** + مجموعه محلی `@iconify-json/lucide` |
| بین‌المللی‌سازی | **@nuxtjs/i18n** — فارسی پیش‌فرض، آماده برای انگلیسی |
| تم روشن/تاریک | **@nuxtjs/color-mode** (کلاس `.dark`) |
| فونت | **Vazirmatn Variable** — باندل محلی با `@fontsource-variable/vazirmatn` |
| حالت اپ | کوکی‌محور (سازگار با SSR) — آماده برای اتصال به بک‌اند واقعی |
| کیفیت کد | ESLint (پیکربندی رسمی `@nuxt/eslint`) + `vue-tsc` |
| مدیریت بسته | pnpm |

> تمام وابستگی‌های زمان اجرا (فونت و آیکون) به‌صورت **محلی باندل می‌شوند** تا اپ
> بدون اتصال به سرویس‌های خارجی نیز کامل کار کند.

---

## ۳. معماری

```
├── app/                      # سورس اپلیکیشن (ساختار Nuxt 4)
│   ├── app.vue               # ریشه؛ پوشش‌دهنده <UApp> برای توست‌ها
│   ├── layouts/              # default (شل اصلی) و auth (ورود/ثبت‌نام/آنبردینگ)
│   ├── pages/
│   │   ├── index.vue         # داشبورد «الان در ساختمان چه خبر است؟»
│   │   ├── auth/             # login، register
│   │   ├── building/         # index (نمای کلی)، units، members، invite
│   │   ├── announcements/    # index، new، [id]، [id]/edit
│   │   ├── problems/         # index، new، [id]
│   │   ├── charges/          # index، new، [id]
│   │   ├── expenses/         # index، new
│   │   ├── finances/         # نمای شفافیت مالی ساختمان
│   │   ├── services/         # index (دایره خدمات)، [id] (جزئیات و تماس)
│   │   ├── onboarding.vue    # ویزارد ۳ مرحله‌ای ساخت ساختمان
│   │   └── join.vue          # پیوستن با کد دعوت
│   ├── components/           # سیستم طراحی + دیالوگ‌های دامنه
│   │   └── form/             # فیلدهای فرم (بر پایه UFormField)
│   ├── composables/          # useAuth، useAppStore، useStoreRefs، useTheme، …
│   ├── middleware/           # auth.global — بذرپاشی، گاردها و ریدایرکت‌ها
│   ├── utils/                # format (ارقام/تاریخ/مبلغ)، image (ذخیره تصاویر)، clipboard، misc
│   ├── types/                # تایپ‌های دامنه (Building، Announcement، ProblemReport، …)
│   ├── data/                 # mock.ts (متادیتای دسته‌ها)، providers.ts (دایره خدمات) و seed.ts (دیتای دمو)
│   └── assets/css/main.css   # Tailwind v4 + توکن‌ها + تم
├── i18n/locales/             # fa.json (فعال)، en.json (اسکلت برای آینده)
├── public/favicon.svg
├── nuxt.config.ts
└── app.config.ts
```

### الگوهای کلیدی

- **لایه حالت (`useStoreRefs` + `useAppStore` + `useAuth`)**: همه داده‌ها (کاربر، کاربران،
  ساختمان‌ها، واحدها، اعضا، دعوت‌نامه‌ها، اطلاعیه‌ها، گزارش مشکلات) در کوکی‌ها ذخیره
  می‌شوند تا در SSR و کلاینت هم‌خوان بمانند (بدون عدم‌تطابق هیدریشن). رفرنس‌های کوکی در
  هر ریکوئست فقط یک‌بار ساخته می‌شوند. کوکی‌های پرمحتوا (اطلاعیه‌ها/گزارش‌ها) با پایه۶۴
  کدگذاری می‌شوند. با اتصال بک‌اند واقعی، فقط همین لایه جایگزین می‌شود.
- **داشبورد**: صفحه خانه برای کاربر دارای ساختمان به پرسش «الان در ساختمان چه خبر است؟»
  پاسخ می‌دهد: خوش‌آمدگویی، ساختمان/واحد فعلی، دسترسی سریعِ وابسته به نقش، تازه‌ترین
  اطلاعیه‌ها، مشکلاتِ رسیدگی‌نشده و کارت شارژ. کاربر بدون ساختمان یا واردنشده، نمای
  معرفی/هدایت می‌بیند.
- **دایره خدمات**: فهرست ارائه‌دهندگان، داده نمایشی پلتفرم است (`app/data/providers.ts`)
  تا کوکی‌ها کوچک بمانند؛ فقط شناسه‌های «مورد اعتماد» هر ساختمان در کوکی پایه۶۴
  `ham-trusted-providers` ذخیره می‌شود. جستجو و فیلتر دسته‌بندی سمت کلاینت روی همین
  فهرست انجام می‌شود و تماس با لینک `tel:` مستقیم است (بدون رزرو/پرداخت).
- **ناوبری**: یک منبع واحد (`useAppNavigation`) هم ناوبری پایین موبایل و هم
  ناوبری افقی دسکتاپ را تغذیه می‌کند.
- **گاردها**: میدلور سراسری `auth.global` — بذرپاشی دیتای دمو، محافظت از مسیرهای
  داخلی، تفکیک دسترسی مدیر/ساکن و ذخیره کد دعوتِ در انتظار برای کاربر واردنشده.

---

## ۴. احراز هویت و نقش‌ها

### نقش‌ها

| نقش | توانایی‌ها |
| --- | --- |
| **مدیر ساختمان** | ساخت ساختمان، افزودن/ویرایش/حذف واحدها، افزودن/حذف اعضا، تخصیص عضو به واحد، ساخت و مدیریت دعوت‌نامه‌ها، ویرایش اطلاعات ساختمان، ایجاد/ویرایش/حذف اطلاعیه‌ها، مشاهده همه گزارش‌های مشکلات و تغییر وضعیت آن‌ها، ایجاد/حذف شارژ، ثبت دستی پرداخت اعضا، ثبت/حذف هزینه‌ها، نشان‌گذاری «مورد اعتماد ساختمان» روی ارائه‌دهندگان خدمات |
| **ساکن** | مشاهده نمای کلی ساختمان، واحدها و اعضا، تماس با مدیر، پیوستن با کد دعوت، مشاهده اطلاعیه‌ها، ثبت گزارش مشکل و پیگیری گزارش‌های خودش، مشاهده شارژها/هزینه‌ها و وضعیت پرداخت خودش، مشاهده نمای شفافیت مالی، جستجو در دایره خدمات و تماس مستقیم با ارائه‌دهندگان |

### معماری ورود

- فرم ورود با **رمز عبور** فعال است و تب **کد یک‌بارمصرف (OTP)** در معماری آماده نگه
  داشته شده (`AuthMethod` در تایپ‌ها و `AUTH_METHODS` در `useAuth`) — بدون پیچیدگی اضافه.
- ثبت‌نام با انتخاب نقش (مدیر/ساکن) انجام می‌شود؛ مدیر به آنبردینگ و ساکن به صفحه
  پیوستن هدایت می‌شود.
- حساب‌های دمو: **مدیر** `09121112233` و **ساکن** `09123456789` (رمز: `1234`).

### آنبردینگ مدیر (۳ مرحله)

۱. **ساخت ساختمان** — نام، آدرس، تعداد واحدها، توضیحات اختیاری (واحدها خودکار با
الگوی هر طبقه ۲ واحد ساخته می‌شوند)
۲. **افزودن واحدها** — فهرست واحدهای تولیدشده با امکان افزودن/ویرایش
۳. **دعوت ساکنان** — ساخت خودکار کد دعوت با لینک قابل اشتراک‌گذاری

### سیستم دعوت

- کد دعوت ۶ کاراکتری (بدون کاراکترهای مبهم)، اعتبار یک‌ساله، وضعیت فعال/استفاده‌شده/منقضی
- لینک قابل اشتراک‌گذاری `/join?code=XXXXXX` + دکمه‌های کپی کد، کپی لینک و اشتراک‌گذاری
  (`navigator.share` با فالبک کپی)
- کاربر واردنشده‌ای که لینک را باز کند، کد برایش ذخیره شده و پس از ورود/ثبت‌نام
  خودکار به ادامه جریان پیوستن برمی‌گردد
- کد دعوت دمو برای تست: **`K7X2M9`**

---

## ۵. مدل‌های دامنه

| مدل | فیلدهای اصلی |
| --- | --- |
| `AuthUser` | id، name، phone، role، password (نمایشی)، createdAt |
| `Building` | id، name، address، unitsCount، description?، managerId، createdAt |
| `BuildingUnit` | id، buildingId، number، floor |
| `BuildingMember` | id، buildingId، userId?، name، phone?، role، unitId?، unitStatus? (مالک/مستأجر)، joinedAt |
| `Invitation` | id، buildingId، code، role، createdBy، createdAt، expiresAt، status |
| `Announcement` | id، buildingId، title، body، importance (عادی/مهم)، image? (کلید مرجع)، createdBy، createdByName، createdAt، updatedAt? |
| `ProblemReport` | id، buildingId، category، title، description، image?، status (جدید/در حال پیگیری/حل شده)، reportedBy، reportedByName، createdAt، updatedAt? |
| `BuildingCharge` | id، buildingId، title، period، amount (تومان)، dueAt، notes?، createdBy، createdByName، createdAt |
| `ChargePayment` | id، chargeId، memberId، memberName، amount (تومان)، paidAt، note?، method (`manual`؛ آماده برای `online`)، recordedBy |
| `Expense` | id، buildingId، title، amount (تومان)، category، date، notes?، receipt? (کلید مرجع)، createdBy، createdByName، createdAt |
| `ServiceProvider` | id، name، category (۱۱ دسته)، description، rating (از ۵)، phone، serviceArea، workingHours، image? |

قاعده: هر واحد حداکثر یک عضو اصلی دارد؛ تخصیص واحد به عضو جدید، عضو قبلی را آزاد می‌کند.
هر شارژ یک سهم مساوی برای هر عضو ساختمان است و هر عضو برای هر شارژ حداکثر یک رکورد پرداخت دارد.

### اطلاعیه‌ها (Announcements)

- **مدیر** اطلاعیه می‌سازد، ویرایش و حذف می‌کند؛ **ساکن** فقط فهرست و جزئیات را می‌بیند.
- اهمیت دو سطح دارد: **عادی** (نشان خنثی) و **مهم** (نشان هشدار). تصویر اختیاری است.
- مسیرها: `/announcements` (فهرست)، `/announcements/new`، `/announcements/[id]`، `/announcements/[id]/edit`.
- ایجاد/ویرایش فقط برای مدیر؛ میدلور نوشتن روی این مسیرها را برای ساکن به `/announcements` برمی‌گرداند.

### گزارش مشکلات (Problem Reports)

- **ساکن** مشکل گزارش می‌دهد و فقط گزارش‌های خودش را می‌بیند؛ **مدیر** همه گزارش‌ها را
  می‌بیند و وضعیت را تغییر می‌دهد. این ماژول عمداً ساده است (نه سیستم تیکتینگ پیچیده).
- هفت دسته‌بندی: آب، برق، آسانسور، گاز، مشاعات، نظافت، سایر (هرکدام آیکون و رنگ مستقل).
- سه وضعیت: **جدید**، **در حال پیگیری**، **حل شده** — تغییر وضعیت فقط توسط مدیر.
- مسیرها: `/problems` (فهرست + فیلتر وضعیت/دسته)، `/problems/new`، `/problems/[id]`.
- گزارشِ کاربر دیگر برای ساکن در صفحه جزئیات با حالت «دسترسی مجاز نیست» نمایش می‌یابد
  و عنوان آن در متادیتای سئو نیز افشا نمی‌شود.

### شارژ ساختمان (Charges)

- این ماژول عمداً **حسابداری نیست**؛ فقط یک مدل ساده و قابل‌فهم برای شارژ ماهانه.
- **مدیر** شارژ می‌سازد (عنوان، مبلغ هر واحد، دوره، تاریخ سررسید، توضیحات اختیاری)
  و می‌تواند آن را حذف کند. هر عضو ساختمان یک سهم با مبلغ یکسان دارد.
- **وضعیت پرداخت برای هر عضو مشتق می‌شود** (ذخیره نمی‌شود): اگر رکورد پرداخت دارد
  «پرداخت شده»، اگر سررسید گذشته و پرداخت ندارد «دیرکرد»، و در غیر این صورت «پرداخت نشده».
- **مدیر** در صفحه جزئیات هر شارژ، فهرست اعضا با وضعیت پرداخت را می‌بیند و پرداخت را
  به‌صورت **دستی** ثبت می‌کند (مبلغ، تاریخ پرداخت، یادداشت اختیاری) یا رکورد را حذف می‌کند.
  فیلد `method` روی رکورد پرداخت برای افزودن **پرداخت آنلاین در آینده** باز است ولی هنوز درگاهی پیاده نشده است.
- **ساکن** شارژهای فعلی و قبلی، مبلغ، سررسید و وضعیت خودش را می‌بیند و در داشبورد
  کارت شارژ پرداخت‌نشده/دیرکرد یا «حساب تسویه» دارد.
- مسیرها: `/charges` (فهرست)، `/charges/new`، `/charges/[id]`.

### هزینه‌ها و شفافیت مالی (Expenses & Financial Transparency)

- **مدیر** هزینه ثبت می‌کند (عنوان، مبلغ، دسته‌بندی، تاریخ، توضیحات، تصویر رسید اختیاری)
  و می‌تواند آن را حذف کند. هفت دسته‌بندی: آب، گاز، برق، آسانسور، نظافت، تعمیرات، سایر.
- هزینه‌ها برای **همه اعضا** (شفاف) نمایش داده می‌شوند؛ حذف فقط توسط مدیر.
- صفحه `/finances` نمای شفافیت مالی است: **درآمد شارژ** (جمع پرداخت‌ها)، **هزینه‌ها**
  (جمع هزینه‌ها)، **مانده** (درآمد منهای هزینه)، **پرداخت‌شده/پرداخت‌نشده** (سهم‌ها)،
  **هزینه‌های اخیر** و پیشرفت دریافت هر شارژ. این خلاصه در داشبورد مدیر نیز ادغام شده است.
- منطق خلاصه مالی در `useAppStore.financialSummary()` متمرکز است تا محاسبات تکراری نشود.
- همه مبالغ با ابزارهای **تومان** در `app/utils/format.ts` فرمت می‌شوند: `formatAmount`
  (جداکننده هزارگان فارسی)، `formatPrice` (مبلغ + «تومان») و `parseAmount` (تحمل ارقام
  فارسی/عربی و جداکننده‌ها هنگام خواندن ورودی کاربر). از جدول‌های فشرده خبری نیست و
  همه‌چیز با کارت‌های موبایل‌پسند نمایش داده می‌شود.

### دایره خدمات ساختمان (Service Directory)

- جریان ساده **پیدا کن ← اطلاعات را ببین ← تماس بگیر**؛ بدون رزرو، سفارش، پرداخت
  یا داشبورد ارائه‌دهنده (عمداً ساده نگه داشته شده است).
- صفحه خانه `/services`: جستجو (نام/دسته/محدوده/شرح)، فیلتر ۱۱ دسته‌بندی با چیپ‌های
  اسکرول‌افقی (لوله‌کشی، برق‌کاری، آسانسور، نظافت، نقاشی، کولر، پکیج و موتورخانه،
  تأسیسات، شیشه‌کاری، کلیدسازی، سایر) و کارت‌های ارائه‌دهنده با امتیاز و دکمه تماس.
- صفحه جزئیات `/services/[id]`: همه اطلاعات (تصویر اختیاری، دسته، امتیاز، شرح، محدوده،
  ساعت کاری، شماره تماس)، اقدام برجسته چسبان **«تماس با خدماتی»** با لینک `tel:` و
  سوئیچ «مورد اعتماد ساختمان» فقط برای مدیر.
- **«مورد اعتماد ساختمان»**: مدیر با یک سوئیچ، ارائه‌دهنده را به ساکنین پیشنهاد می‌دهد؛
  نشان طلایی روی کارت و جزئیات و اولویت در مرتب‌سازی. فقط شناسه‌ها برای هر ساختمان در
  کوکی ذخیره می‌شوند تا بودجه حجم کوکی حفظ شود.
- مسیرها با میدلور محافظت می‌شوند (ورود + عضویت در ساختمان) و شناسه نامعتبر، حالت
  «ارائه‌دهنده پیدا نشد» را نشان می‌دهد.

### تصاویر آپلودی

تصاویر اطلاعیه‌ها، گزارش‌ها و **رسید هزینه‌ها** به‌صورت **دیتای فشرده‌شده** (با `canvas`،
حداکثر ~۱۲۸۰ پیکسل) در `localStorage` مرورگر ذخیره می‌شوند و رکوردها فقط یک **کلید مرجع**
با پیشوند `img:` نگه می‌دارند — چون کوکی‌ها ظرفیت محدودی دارند. این منطق در
`app/utils/image.ts` (`saveImage / loadImage / removeImage / readImageFile / resolveImagePayload`) متمرکز است.

### کوکی‌های پرمحتوا (پایه۶۴)

متن فارسی در کدگذاری درصدیِ پیش‌فرضِ کوکی ~۳ برابر حجم می‌گیرد و مجموعه‌های پرمحتوا را
از سقف ~۴ کیلوبایت عبور می‌داد. کوکی‌های اطلاعیه‌ها، گزارش‌ها، شارژها، پرداخت‌ها و هزینه‌ها
با کدگذاری **پایه۶۴** (`useBase64JsonCookie` در `useStoreRefs`) ذخیره می‌شوند که حجم را
تقریباً نصف می‌کند و بقیه کوکی‌ها همان کدگذاری پیش‌فرض را دارند.

---

## ۶. سیستم طراحی

### رنگ‌ها

| توکن | مقدار | کاربرد |
| --- | --- | --- |
| `primary` | **teal** (تیله‌ای آرام) | اقدامات اصلی، حالت فعال، برند |
| `neutral` | **slate** | متن، حاشیه، سطوح |
| `success` | green | پرداخت شده / انجام شد / دعوت فعال |
| `warning` | amber | در انتظار / مهم |
| `error` | red | فوری / لغو / خطا / منقضی |
| `info` | blue | در حال انجام / اطلاع‌رسانی / ساکن |

نگاشت `primary → teal` از طریق متغیرهای `--ui-color-primary-*` در `main.css` انجام می‌شود.

### سطوح و سایه

- کارت‌ها: `rounded-2xl`، پس‌زمینه سفید، `shadow-sm` + `ring-1 ring-slate-950/5`
- فهرست‌ها به‌جای جدول فشرده، با **کارت و ردیف‌های خوانا** در موبایل نمایش داده می‌شوند
- پس‌زمینه صفحه: `slate-50` (روشن) / `slate-950` (تاریک)
- بدون گرادیان‌های پرحجم، بدون گلس‌مورفیسم — فقط سایه‌های نرم و فضای تمیز

### تایپوگرافی

- فونت: **Vazirmatn Variable** (باندل محلی، بدون درخواست شبکه)
- سلسله‌مراتب: عنوان صفحه `text-xl font-extrabold`، عنوان بخش `text-base font-bold`،
  بدنه `text-sm`، متادیتا `text-[11px]`
- کدها و شماره‌ها (کد دعوت، شماره موبایل، لینک‌ها) با `dir="ltr"` نمایش داده می‌شوند

### فاصله‌گذاری

- ریتم پایه ۴ پیکسل؛ صفحه موبایل `max-w-md px-4`؛ دسکتاپ `max-w-3xl`
- هدر بالا ۶۴ پیکسل چسبان؛ ناوبری پایین ۴ آیتمه با پشتیبانی `safe-area`

---

## ۷. کامپوننت‌ها

### سیستم طراحی پایه

| کامپوننت | نقش |
| --- | --- |
| `AppHeader` | هدر چسبان: لوگو، ناوبری دسکتاپ، تغییر تم، اعلان‌ها، وضعیت کاربر (آواتار/ورود) |
| `AppBottomNavigation` | ناوبری پایین موبایل (تا `lg` مخفی) |
| `PageHeader` / `SectionHeader` | عنوان صفحه/بخش + اسلات اقدامات و «مشاهده همه» |
| `AppCard` | کارت پایه با اندازه‌های پدینگ و حالت هاور |
| `EmptyState` / `LoadingState` / `ErrorState` | حالت‌های خالی/بارگذاری/خطا (اسکلت و تلاش مجدد) |
| `StatusBadge` | نشان وضعیت با نگاشت `وضعیت → رنگ + برچسب ترجمه‌شده` (نقش‌ها، دعوت‌نامه، اهمیت اطلاعیه، وضعیت گزارش) |
| `UserAvatar` | آواتار با حروف اختصاری فارسی و اندازه‌بندی |
| `ConfirmDialog` | دیالوگ تأیید با لحن `primary/danger` |
| `BaseTextField` / `BaseTextAreaField` / `BaseSelectField` / `BaseSwitchField` | فیلدهای فرم با لیبل، راهنما و خطا (بر پایه `UFormField`)؛ پشتیبانی `dir` برای ورودی‌های عددی/کد |
| `BaseImageField` | فیلد تصویر اختیاری: انتخاب فایل، پیش‌نمایش، حذف، فشرده‌سازی و پیام خطای نوع/حجم |
| `AppImage` | نمایش تصویر با حالت اسکلت/خطا؛ پذیرش دیتای مستقیم یا کلید مرجع `img:` (سازگار با SSR) |

### کامپوننت‌های دامنه

| کامپوننت | نقش |
| --- | --- |
| `AppStepper` | نشانگر مرحله‌ای آنبردینگ (ساخت → واحدها → دعوت) |
| `UnitFormDialog` | افزودن/ویرایش واحد با اعتبارسنجی شماره تکراری |
| `UnitAssignDialog` | تخصیص ساکن به واحد (از صفحه واحدها) |
| `MemberFormDialog` | افزودن عضو با نقش و واحد اختیاری |
| `AssignUnitDialog` | تخصیص واحد به عضو (از صفحه اعضا) |
| `InvitationCard` | نمایش کد دعوت + لینک + کپی/اشتراک‌گذاری |
| `AnnouncementForm` | فرم مشترک ایجاد/ویرایش اطلاعیه (عنوان، متن، اهمیت، تصویر اختیاری) |
| `PaymentRecordDialog` | ثبت دستی پرداخت شارژ یک عضو (مبلغ، تاریخ پرداخت، یادداشت اختیاری) |
| `ProviderCard` | کارت ارائه‌دهنده خدمات: آواتار دسته، نام، نشان «مورد اعتماد»، امتیاز، محدوده و دکمه تماس `tel:` |

---

## ۸. راست‌به‌چپ و بین‌المللی‌سازی

- `html` با `lang="fa" dir="rtl"` رندر می‌شود؛ جهت از طریق لوکل i18n مدیریت می‌شود.
- در سراسر کد فقط از کلاس‌های **منطقی** تیلویند استفاده شده است: `ps/pe/ms/me/start/end`
  به‌جای `left/right` تا با افزودن زبان‌های چپ‌به‌چپ، چیدمان خودکار آینه شود.
- ارقام و تاریخ‌ها با `Intl` و لوکل `fa-IR` تولید می‌شوند (تقویم جلالی خودکار).
- **افزودن انگلیسی**: اسکلت `i18n/locales/en.json` آماده است؛ کافی است لوکل `en`
  با `dir: ltr` به `nuxt.config.ts` اضافه و ترجمه‌ها کامل شوند.

---

## ۹. تم روشن / تاریک

- معماری کامل با `@nuxtjs/color-mode` و واریانت `dark:` تیلویند (استراتژی کلاس).
- تم پیش‌فرض: **روشن** (پولیش‌شده)؛ تم تاریک روی همه صفحات پشتیبانی می‌شود
  و از دو مسیر قابل تغییر است: دکمه هدر و کلید «حالت تاریک» در صفحه حساب من.

---

## ۱۰. کیفیت و تست

| دستور | شرح |
| --- | --- |
| `pnpm dev` | سرور توسعه |
| `pnpm build` | بیلد پروداکشن (SSR) |
| `pnpm typecheck` | بررسی تایپ با `vue-tsc` |
| `pnpm lint` | بررسی لینتر (ESLint) |

وضعیت فعلی: ✅ بیلد موفق • ✅ تایپ‌چک بدون خطا • ✅ لینتر بدون خطا

سناریوهای تست‌شده (رندر سرور + گاردها + ریدایرکت‌ها):

- ورود/ثبت‌نام هر دو نقش و هدایت صحیح پس از ورود (آنبردینگ برای مدیر بدون ساختمان،
  پیوستن برای ساکن بدون ساختمان، خانه برای کاربر دارای ساختمان)
- دسترسی‌ها: صفحات مدیریت برای ساکن و کاربر واردنشده مسدود/محدود می‌شوند
- جریان کامل مدیر: نمای کلی با اقدامات زمینه‌ای، افزودن/ویرایش/حذف واحد، تخصیص ساکن،
  افزودن/حذف عضو، ساخت/حذف دعوت‌نامه
- جریان پیوستن: کد از لینک پیش‌پر می‌شود، پیش‌نمایش ساختمان، خطاهای کد نامعتبر/منقضی/تکراری
- **اطلاعیه‌ها**: فهرست/جزئیات برای هر دو نقش، ایجاد/ویرایش/حذف فقط برای مدیر؛ ساکن از
  `/announcements/new` و `/edit` به فهرست برمی‌گردد؛ حالت «اطلاعیه پیدا نشد» برای شناسه نامعتبر
- **گزارش مشکلات**: فهرست با فیلتر وضعیت/دسته، ایجاد گزارش، مشاهده گزارشِ کاربرِ دیگر توسط
  ساکن با حالت «دسترسی مجاز نیست»، تغییر وضعیت فقط برای مدیر، عدم افشای عنوان گزارش غیرمجاز در متا
- **شارژ**: ایجاد شارژ (عنوان/مبلغ/دوره/سررسید/توضیحات)، حذف شارژ همراه با پرداخت‌هایش،
  وضعیت مشتق‌شده برای هر عضو (پرداخت‌نشده/پرداخت‌شده/دیرکرد بر اساس سررسید)،
  ثبت دستی پرداخت توسط مدیر و حذف رکورد پرداخت، نمای ساکن از پرداخت خودش
- **هزینه‌ها**: ثبت هزینه با دسته‌بندی/تاریخ/رسید اختیاری، حذف فقط برای مدیر،
  نمایش شفاف برای همه اعضا
- **شفافیت مالی**: صحت محاسبات (جمع پرداخت‌ها = درآمد، جمع هزینه‌ها، مانده، تعداد
  سهم‌های پرداخت‌شده/پرداخت‌نشده) با داده‌های دمو و داده‌های تزریق‌شده بررسی شد
- **داشبورد مالی**: کارت شارژ پرداخت‌نشده/دیرکرد یا «حساب تسویه» برای ساکن،
  نمای درآمد/هزینه/مانده برای مدیر
- **دایره خدمات**: جستجو و فیلتر دسته‌بندی، مرتب‌سازی «مورد اعتماد» اول، کارت‌ها و صفحه
  جزئیات، لینک‌های `tel:` روی کارت/جزئیات/دکمه چسبان، تغییر «مورد اعتماد» توسط مدیر با
  انعکاس فوری در فهرست و جزئیات، حالت «ارائه‌دهنده پیدا نشد» و «نتیجه‌ای پیدا نشد»،
  گارد مسیرهای خدمات برای ناشناس و کاربر بدون ساختمان
- سازگاری کوکی‌ها با سقف حجم مرورگر (کدگذاری پایه۶۴ برای مجموعه‌های پرمحتوا؛ تصاویر در `localStorage`)
- بدون سرریز افقی در عرض‌های موبایل (فیلترهای دسته‌بندی اسکرول افقی داخلی دارند)

---

## ۱۱. نقشه راه فازهای بعدی

**فاز ۱ — زیرساخت (انجام شد)**
- شل اپلیکیشن، ناوبری، سیستم طراحی، تم روشن/تاریک، صفحات اصلی با دیتای ماک

**فاز ۲ — احراز هویت و مدیریت ساختمان (انجام شد)**
- ورود/ثبت‌نام/خروج با نقش‌های مدیر و ساکن (معماری آماده برای رمز عبور و OTP)
- ساخت ساختمان + آنبردینگ سه‌مرحله‌ای، مدیریت واحدها و اعضا، سیستم دعوت با کد و لینک
- ذخیره‌سازی کوکی‌محورِ سازگار با SSR به‌جای دیتای ماک ثابت

**فاز ۳ — داشبورد، اطلاعیه‌ها و گزارش مشکلات (انجام شد)**
- داشبورد «الان در ساختمان چه خبر است؟» با دسترسی سریع وابسته به نقش
- اطلاعیه‌ها: ایجاد/ویرایش/حذف توسط مدیر (عنوان، متن، اهمیت عادی/مهم، تصویر اختیاری)،
  مشاهده فهرست و جزئیات برای ساکنین
- گزارش مشکلات: ۷ دسته‌بندی، ثبت توسط ساکن/مدیر با تصویر اختیاری، فیلتر وضعیت/دسته،
  مشاهده گزارش‌های خود برای ساکن و همه برای مدیر، تغییر وضعیت توسط مدیر
- تصاویر آپلودی فشرده‌شده در `localStorage` + کوکی‌های پرمحتوا با کدگذاری پایه۶۴

**فاز ۴ — شارژ، هزینه‌ها و شفافیت مالی (انجام شد)**
- شارژ ساختمان: ایجاد/حذف شارژ توسط مدیر، وضعیت پرداخت مشتق‌شده برای هر عضو
  (پرداخت‌نشده/پرداخت‌شده/دیرکرد)، ثبت و حذف دستی پرداخت‌ها
- هزینه‌ها: ثبت با دسته‌بندی/تاریخ/تصویر رسید اختیاری، نمایش شفاف برای همه اعضا
- نمای شفافیت مالی: درآمد شارژ، هزینه‌ها، مانده، سهم‌های پرداخت‌شده/پرداخت‌نشده،
  هزینه‌های اخیر + ادغام خلاصه مالی در داشبورد مدیر و کارت شارژ در داشبورد ساکن
- معماری آماده برای پرداخت آنلاین (فیلد `method` در رکورد پرداخت) بدون پیاده‌سازی درگاه

**فاز ۵ — دایره خدمات ساختمان (انجام شد)**
- دایره ارائه‌دهندگان خدمات با جریان «پیدا کن، ببین، تماس بگیر»: جستجو، فیلتر ۱۱ دسته‌بندی،
  کارت و صفحه جزئیات با لینک `tel:` و اقدام برجسته چسبان «تماس با خدماتی»
- «مورد اعتماد ساختمان»: سوئیچ مدیر، نشان طلایی و اولویت در مرتب‌سازی؛ بدون رزرو/پرداخت/داشبورد ارائه‌دهنده

**فاز ۶ — بک‌اند واقعی**
- جایگزینی لایه کوکی با API (احراز هویت پیامکی/OTP واقعی، پایگاه داده، نقش سرایداری، دریافت دایره خدمات از سرور)

**فاز ۷ — پرداخت آنلاین**
- درگاه پرداخت شارژ (اتصال `method: 'online'`)، یادآور سررسید، تاریخچه پرداخت هر واحد

**فاز ۸ — تعاملات**
- رأی‌گیری و نظرسنجی ساکنین، چت ساختمان، اعلان‌های لحظه‌ای (Push)

**فاز ۹ — تکمیل**
- زبان انگلیسی، حالت سوپرپپ برای مدیران، خروجی گزارش‌ها، نسخه نصبی (PWA)

# هم‌ساختمان 🏢

اپلیکیشن مدرن و فارسی مدیریت ساختمان — برای ساکنین، مالکین و مدیران ساختمان.

---

## ۱. چشم‌انداز محصول

زندگی آپارتمانی با چالش‌های تکراری همراه است: پرداخت دیرهنگام شارژ، بی‌خبری از اطلاعیه‌ها،
هماهنگ‌نشدن تعمیرات و نبود شفافیت مالی. **هم‌ساختمان** این تجربه را به یک اپ ساده، شفاف و دوستانه تبدیل می‌کند:

- **پرداخت شارژ** در چند ثانیه با وضعیت شفاف هر دوره
- **اطلاعیه‌های ساختمان** با دسته‌بندی و اولویت (اطلاع‌رسانی / مهم / فوری)
- **درخواست و پیگیری خدمات** (تأسیسات، برق، نظافت، آسانسور و…) با وضعیت لحظه‌ای
- **شناخت ساختمان و ساکنین**: واحدها، طبقات، امکانات و راه‌های ارتباطی با مدیر

اصول محصول:

| اصل | معنا |
| --- | --- |
| سادگی | هر کار ضروری حداکثر ۲–۳ لمس |
| اعتماد | شفافیت مالی و وضعیت‌های روشن |
| موبایل‌فرست | طراحی برای ۳۶۰ تا ۴۳۰ پیکسل، سپس تبلت و دسکتاپ |
| فارسیِ اصیل | راست‌به‌چپ واقعی، ارقام و تقویم جلالی، فونت وزیرمتن |

---

## ۲. پشته فنی

| لایه | فناوری |
| --- | --- |
| فریم‌ورک | **Nuxt 4** (Vue 3، TypeScript، SSR) |
| استایل | **Tailwind CSS v4** (CSS-first، بدون فایل کانفیگ) |
| کتابخانه کامپوننت | **Nuxt UI v4** (Reka UI) با پالت شخصی‌سازی‌شده |
| آیکون‌ها | **@nuxt/icon** + مجموعه محلی `@iconify-json/lucide` |
| بین‌المللی‌سازی | **@nuxtjs/i18n** — فارسی پیش‌فرض، آماده برای انگلیسی |
| تم روشن/تاریک | **@nuxtjs/color-mode** (کلاس `.dark`) |
| فونت | **Vazirmatn Variable** — باندل محلی با `@fontsource-variable/vazirmatn` |
| کیفیت کد | ESLint (پیکربندی رسمی `@nuxt/eslint`) + `vue-tsc` |
| مدیریت بسته | pnpm |

> تمام وابستگی‌های زمان اجرا (فونت و آیکون) به‌صورت **محلی باندل می‌شوند** تا اپ
> بدون اتصال به سرویس‌های خارجی نیز کامل کار کند.

---

## ۳. معماری

```
├── app/                      # سورس اپلیکیشن (ساختار Nuxt 4)
│   ├── app.vue               # ریشه؛ پوشش‌دهنده <UApp> برای توست‌ها
│   ├── layouts/default.vue   # شل اصلی: هدر + محتوا + ناوبری پایین
│   ├── pages/                # خانه، ساختمان، خدمات، حساب من
│   ├── components/           # کامپوننت‌های سیستم طراحی
│   │   └── form/             # فیلدهای فرم (بر پایه UFormField)
│   ├── composables/          # useTheme، useAppNavigation، useMockApi
│   ├── utils/                # format.ts (ارقام/تاریخ/مبلغ)، misc.ts
│   ├── types/                # تایپ‌های دامنه (Unit، Charge، …)
│   ├── data/mock.ts          # دیتای ماک واقع‌گرایانه
│   └── assets/css/main.css   # Tailwind v4 + توکن‌ها + تم
├── i18n/locales/             # fa.json (فعال)، en.json (اسکلت برای آینده)
├── public/favicon.svg
├── nuxt.config.ts
└── app.config.ts
```

### الگوهای کلیدی

- **لایه داده**: `useMockApi` دریافت از سرور را با `useAsyncData` شبیه‌سازی می‌کند
  (وضعیت‌های `pending / error / data` و اسکلت/خطا به‌صورت طبیعی استفاده می‌شوند).
  در فازهای بعد بدون تغییر رابط صفحات، با فراخوانی‌های واقعی جایگزین می‌شود.
- **ناوبری**: یک منبع واحد (`useAppNavigation`) هم ناوبری پایین موبایل و هم
  ناوبری افقی دسکتاپ را تغذیه می‌کند.
- **صفحات**: محتوای متغیر (اعلامیه‌ها، واحدها، درخواست‌ها) داده‌محور است و
  متن‌های ثابت رابط از i18n می‌آیند.

---

## ۴. سیستم طراحی

### رنگ‌ها

| توکن | مقدار | کاربرد |
| --- | --- | --- |
| `primary` | **teal** (تیله‌ای آرام) | اقدامات اصلی، حالت فعال، برند |
| `neutral` | **slate** | متن، حاشیه، سطوح |
| `success` | green | پرداخت شده / انجام شد |
| `warning` | amber | در انتظار / مهم |
| `error` | red | فوری / لغو / خطا |
| `info` | blue | در حال انجام / اطلاع‌رسانی |

نگاشت `primary → teal` از طریق متغیرهای `--ui-color-primary-*` در `main.css` انجام می‌شود.

### سطوح و سایه

- کارت‌ها: `rounded-2xl`، پس‌زمینه سفید، `shadow-sm` + `ring-1 ring-slate-950/5`
- پس‌زمینه صفحه: `slate-50` (روشن) / `slate-950` (تاریک)
- بدون گرادیان‌های پرحجم، بدون گلس‌مورفیسم — فقط سایه‌های نرم و فضای تمیز

### تایپوگرافی

- فونت: **Vazirmatn Variable** (باندل محلی، بدون درخواست شبکه)
- سلسله‌مراتب: عنوان صفحه `text-xl font-extrabold`، عنوان بخش `text-base font-bold`،
  بدنه `text-sm`، متادیتا `text-[11px]`

### فاصله‌گذاری

- ریتم پایه ۴ پیکسل؛ صفحه موبایل `max-w-md px-4`؛ دسکتاپ `max-w-3xl`
- هدر بالا ۶۴ پیکسل چسبان؛ ناوبری پایین ۴ آیتمه با پشتیبانی `safe-area`

---

## ۵. کامپوننت‌های سیستم طراحی

| کامپوننت | نقش |
| --- | --- |
| `AppHeader` | هدر چسبان: لوگو، ناوبری دسکتاپ، تغییر تم، اعلان‌ها |
| `AppBottomNavigation` | ناوبری پایین موبایل (تا `lg` مخفی) |
| `PageHeader` | عنوان + توضیح صفحه + اسلات اقدامات |
| `SectionHeader` | عنوان بخش + لینک «مشاهده همه» |
| `AppCard` | کارت پایه با اندازه‌های پدینگ و حالت هاور |
| `EmptyState` / `LoadingState` / `ErrorState` | حالت‌های خالی/بارگذاری/خطا (اسکلت و تلاش مجدد) |
| `StatusBadge` | نشان وضعیت با نگاشت `وضعیت → رنگ + برچسب ترجمه‌شده` |
| `UserAvatar` | آواتار با حروف اختصاری فارسی و اندازه‌بندی |
| `ConfirmDialog` | دیالوگ تأیید با لحن `primary/danger` |
| `BaseTextField` / `BaseTextAreaField` / `BaseSelectField` / `BaseSwitchField` | فیلدهای فرم با لیبل، راهنما و خطا (بر پایه `UFormField`) |

---

## ۶. راست‌به‌چپ و بین‌المللی‌سازی

- `html` با `lang="fa" dir="rtl"` رندر می‌شود؛ جهت از طریق لوکل i18n مدیریت می‌شود.
- در سراسر کد از کلاس‌های منطقی تیلویند استفاده شده است: `ps/pe/ms/me/start/end`
  به‌جای `left/right` تا با افزودن زبان‌های چپ‌به‌چپ، چیدمان خودکار آینه شود.
- ارقام و تاریخ‌ها با `Intl` و لوکل `fa-IR` تولید می‌شوند (تقویم جلالی خودکار).
- **افزودن انگلیسی**: اسکلت `i18n/locales/en.json` آماده است؛ کافی است لوکل `en`
  با `dir: ltr` به `nuxt.config.ts` اضافه و ترجمه‌ها کامل شوند.

---

## ۷. تم روشن / تاریک

- معماری کامل با `@nuxtjs/color-mode` و واریانت `dark:` تیلویند (استراتژی کلاس).
- تم پیش‌فرض: **روشن** (پولیش‌شده)؛ تم تاریک روی همه صفحات پشتیبانی می‌شود
  و از دو مسیر قابل تغییر است: دکمه هدر و کلید «حالت تاریک» در صفحه حساب من.

---

## ۸. کیفیت

| دستور | شرح |
| --- | --- |
| `pnpm dev` | سرور توسعه |
| `pnpm build` | بیلد پروداکشن (SSR) |
| `pnpm typecheck` | بررسی تایپ با `vue-tsc` |
| `pnpm lint` | ESLint با پیکربندی رسمی Nuxt |

وضعیت فعلی: ✅ بیلد موفق • ✅ تایپ‌چک بدون خطا • ✅ لینتر بدون خطا

---

## ۹. نقشه راه فازهای بعدی

**فاز ۱ — زیرساخت (انجام شد)**
- شل اپلیکیشن، ناوبری، سیستم طراحی، تم روشن/تاریک، صفحات اصلی با دیتای ماک

**فاز ۲ — بک‌اند و احراز هویت**
- احراز هویت ساکنین (ورود با موبایل)، نقش‌ها (ساکن/مدیر/سرایدار)
- جایگزینی `useMockApi` با API واقعی

**فاز ۳ — مالی**
- درگاه پرداخت شارژ، تاریخچه پرداخت، گزارش هزینه‌ها، یادآور سررسید

**فاز ۴ — تعاملات**
- رأی‌گیری و نظرسنجی ساکنین، چت ساختمان، اعلان‌های لحظه‌ای (Push)

**فاز ۵ — تکمیل**
- زبان انگلیسی، حالت سوپرپاپ برای مدیران، خروجی گزارش‌ها، نسخه نصبی (PWA)
